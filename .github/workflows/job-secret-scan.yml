name: Secret Scan

on:
  workflow_call:

jobs:
  gitleaks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"
      - uses: gitleaks/gitleaks-action@v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITLEAKS_CONFIG: .gitleaks.toml
      - name: Gate check
        if: always()
        run: |
          REPORT=""
          for f in gitleaks-report.json results.sarif; do
            [ -f "$f" ] && REPORT="$f" && break
          done
          [ -n "$REPORT" ] || echo '{"runs":[{"results":[]}]}' > gitleaks-report.json
          [ -n "$REPORT" ] || REPORT=gitleaks-report.json
          POLICY="${SECURITY_POLICY:-config/security-gate-policy.yaml}"
          python scripts/gate-check.py --control secrets --report "$REPORT" --policy "$POLICY"
