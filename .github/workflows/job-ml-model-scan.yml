name: ML Model Scan

on:
  workflow_call:
  workflow_dispatch:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: echo '{"runs":[{"results":[]}]}' > ml-model.sarif
      - run: python scripts/gate-check.py --control ml_model --report ml-model.sarif
