name: Security Linters

on:
  workflow_call:

jobs:
  linter-security:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service: [contracts, worker, api]
    defaults:
      run:
        working-directory: backend/${{ matrix.service }}
    steps:
      - uses: actions/checkout@v5
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
      - name: Install dependencies
        run: uv sync
      - name: Ruff security lint
        run: |
          uv run ruff check . --output-format sarif -o linter.sarif || true
          [ -s linter.sarif ] || echo '{"version":"2.1.0","$schema":"https://raw.githubusercontent.com/oasis-tcs/sarif-spec/master/Schemata/sarif-schema-2.1.0.json","runs":[{"tool":{"driver":{"name":"none","rules":[]}},"results":[]}]}' > linter.sarif
      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"
      - name: Gate check
        working-directory: ${{ github.workspace }}
        run: |
          POLICY="${SECURITY_POLICY:-config/security-gate-policy.yaml}"
          python scripts/gate-check.py --control linters --report backend/${{ matrix.service }}/linter.sarif --policy "$POLICY"
      - uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: backend/${{ matrix.service }}/linter.sarif
          category: linters-${{ matrix.service }}
