name: SAST

on:
  workflow_call:

jobs:
  codeql:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"
# DIAGNOSTIC: stripped to bisect a startup_failure.
