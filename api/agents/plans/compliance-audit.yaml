id: compliance-audit
name: Compliance audit
description: Compliance worker for documents and scheduled audits

routing:
  rules:
    - event_types: [doc.upload, compliance.schedule, finding.reference]
      personas: [compliance]
      notify_control: true
